{{#if session.isAuthenticated}}
  Logged in as {{session.currentUser.email}}
  {{log session.currentUser.email}}
  {{log session.uid}}
  <button {{action "signOut"}}>Sign out</button>
  {{#link-to 'index'}}<h1>ContentLand</h1>{{/link-to}}
  {{outlet}}
{{else}}
  {{user-login signIn="signIn"}}
  {{new-user createUser="createUser"}}
{{/if}}
