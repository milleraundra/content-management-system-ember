import Ember from 'ember';

export function categoryTitle(params) {
  // console.log(params[0].category);
  // var site = params[0];
  // console.log(site);
  // return site.get('category');
}

export default Ember.Helper.helper(categoryTitle);
