import { categoryTitle } from 'cms-ember/helpers/category-title';
import { module, test } from 'qunit';

module('Unit | Helper | category title');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = categoryTitle([42]);
  assert.ok(result);
});
